# Server
server.port=8080

# MongoDB Configuration - URI is injected via environment variable for security
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/employeeDB}

# Jackson - exclude null fields from JSON responses
spring.jackson.default-property-inclusion=non_null

# Logging
logging.level.com.assessmentportal=INFO
